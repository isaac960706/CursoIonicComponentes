export interface Listado {
    icon: string;
    name: string;
    redirectTo: string;
  }
  export interface heroes {
    icon: string;
    name: string;
    redirectTo: string;
  }
  // variables que se se usan para crear el JSOON local de 
  // las opcicones º